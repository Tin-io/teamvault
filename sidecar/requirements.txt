# TeamVault sidecar — Python dependencies
# v0.0 — POC. Install size optimization (ONNX/GGUF embedding) deferred to v0.1.

# Web framework
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0

# Config + content parsing
pyyaml>=6.0
python-frontmatter>=1.0

# Storage: vector (LanceDB) + text (SQLite FTS5 ships with Python)
lancedb>=0.6
pyarrow>=14.0  # lancedb dep

# Embeddings: Nomic Embed v1.5 768d via sentence-transformers
# WARNING: pulls torch (~2.5GB). v0.1 should switch to GGUF or ONNX runtime.
sentence-transformers>=2.7
einops>=0.7  # required by nomic-embed model code

# Git operations (used by git_sync.py + publish flow)
gitpython>=3.1

# MCP server (official Anthropic SDK; stdio transport to Claude Code)
mcp>=1.0

# HTTP client (used by mcp_server.py to talk to the sidecar)
httpx>=0.26
